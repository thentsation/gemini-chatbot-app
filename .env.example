# Opcional: se definida, o app usa esta chave direto e não pede nada ao
# visitante (modo "chave do servidor"). Se deixar em branco, cada visitante
# cola a própria chave na barra lateral (modo BYOK, padrão).
GOOGLE_API_KEY=
