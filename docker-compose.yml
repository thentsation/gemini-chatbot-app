services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: gemini-chatbot-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '8501:8501'
